import * as _playwright_test from '@playwright/test';
import { Page, Browser, BrowserContext } from '@playwright/test';

declare const test: _playwright_test.TestType<_playwright_test.PlaywrightTestArgs & _playwright_test.PlaywrightTestOptions & {
    testControl: {
        page: Page;
        browser: Browser;
        context: BrowserContext;
    };
    manualStep: ((stepName: string, params?: {
        [key: string]: any;
    }) => Promise<void>) & {
        soft: (stepName: string, params?: {
            [key: string]: any;
        }) => Promise<void>;
    };
}, _playwright_test.PlaywrightWorkerArgs & _playwright_test.PlaywrightWorkerOptions>;

interface Config {
    analyticsEnabled: boolean;
    uiPort: number;
}
declare const config: {
    setConfig: (config: Partial<Config>) => void;
    readonly analyticsEnabled: boolean;
    readonly uiPort: number;
};

export { config, test as default };
